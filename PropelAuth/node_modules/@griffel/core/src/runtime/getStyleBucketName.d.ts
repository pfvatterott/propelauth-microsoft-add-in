import type { StyleBucketName } from '@griffel/core/src/types';
import type { AtRules } from '@griffel/core/src/runtime/utils/types';
/**
 * Gets the bucket depending on the pseudo.
 *
 * Input:
 *
 * ```
 * ":hover"
 * ":focus:hover"
 * ```
 *
 * Output:
 *
 * ```
 * "h"
 * "f"
 * ```
 *
 * @internal
 */
export declare function getStyleBucketName(selectors: string[], atRules: AtRules): StyleBucketName;
